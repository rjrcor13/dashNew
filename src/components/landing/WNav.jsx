import { Outlet } from 'react-router';

const WNav = () => {
	return (
		<>
			<Outlet />
			<div></div>
		</>
	);
};

export default WNav;

// import { Outlet } from 'react-router';

// export default () => <Outlet />;
